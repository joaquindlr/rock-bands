To initialize the project you must first install the libraries using: 

yarn install

To run the project, use the command: 

yarn start 

The user accounts for the login are simulated from the front in the address config/users.js

The only loaded account is:

example@example.com
12345678
